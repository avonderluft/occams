- content_for :right_column do
  - link = occams_admin_cms_site_page_revisions_path(@site, @page)
  = render "occams/admin/cms/revisions/sidebar", record: @page, link: link
  = render "occams/admin/cms/translations/sidebar"

.page-header
  %h2= t('.title')

= occams_form_with model: @page, scope: :page, url: {action: :update}, html: {multipart: true} do |form|
  = render form
